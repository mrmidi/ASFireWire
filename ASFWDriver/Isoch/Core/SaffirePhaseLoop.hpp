#pragma once

// Compatibility forwarding header. Saffire/48k TX phase policy lives with the
// direct audio isoch timing layer; keep this include during the staged refactor.
#include "../../AudioEngine/DirectIsoch/Timing/SaffirePhaseLoop.hpp"
