#pragma once

// Compatibility forwarding header. Audio/CIP/SYT sync lives with the direct
// audio isoch pipeline; keep this include during the staged refactor.
#include "../../AudioEngine/DirectIsoch/Sync/ExternalSyncBridge.hpp"
